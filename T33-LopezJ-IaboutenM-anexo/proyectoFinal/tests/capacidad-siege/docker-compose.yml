services:
  siege-test:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: test-siege
    volumes:
      - ../../logs/capacidad/siege:/logs
    networks:
      red_entorno:
        ipv4_address: 192.168.30.106

networks:
  red_entorno:
    external: true
